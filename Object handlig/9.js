// 9. Write a JavaScript program to calculate the area and perimeter of a circle.
const radius = 10;

const area = Math.PI * radius ** 2;
const perimeter = 2 * Math.PI * radius;

console.log(`aera = ${area}`);
console.log(`perimeter = ${perimeter}`);